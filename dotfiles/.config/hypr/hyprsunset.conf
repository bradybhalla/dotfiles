# TODO: review new file (schedule times and temperatures)
# Automatic night mode schedule. Manual toggles from waybar still work and
# apply until the next profile transition. 6500K is neutral (no filter);
# the waybar script treats anything below 5000K as night mode.

profile {
    time = 7:00
    temperature = 6500
}

profile {
    time = 21:00
    temperature = 3600
}
